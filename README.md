configured communication configurator with the use of web sockets

for to start the communicator use the command npm start (default port 8000 used)

open two tabs in your browser and write a message =)
